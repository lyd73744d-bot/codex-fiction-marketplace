# 从四个网文项目拆解出的提示词方法论（已本地化）

只吸收**结构与检查法**，不照搬外仓原文。结合本插件原则：候选先落盘、作者确认才入台账、有积分才调模型。

## 学到的四条主结构

### 1. 入口轻、协议重（天命）
- SKILL 只负责：识别阶段、加载绑定资料、走固定闭环
- 细则放 references，需要时再读
- 每次交付必须有「仪表盘 + 下一令」

本插件映射：
- 入口：`longform-fiction-director/SKILL.md` + `fiction_workflow_guide`
- 协议：本目录 references
- 仪表盘：`delivery-dashboard.md`

### 2. 写前控制卡，不是摘要（Lorn）
- 控制卡回答「这一章怎么写」：冲突、节拍、人物目标、章末钩子、禁止事项
- 先控制卡后正文，避免边写边漂

本插件映射：`chapter-control-card.md`；细纲阶段产出控制卡，再写候选正文。

### 3. 网文专项检查可拆 skill（chinese-webnovel）
- 钩子、爽点、金手指、黄金开篇各自成检查项
- 质检时按项打勾，不空喊“更爽一点”

本插件映射：`hook-shuangdian-checklist.md` 并入质检。

### 4. 去 AI 味要分级、改味不改剧（Daisy deslop）
- 先扫描再改；轻度/中度/重度不同删改上限
- 只改说法，不改剧情、伏笔、人设

本插件映射：`deslop-grades.md` + `humanizer-zh`

## 本插件坚持不动的差异

1. 默认一步步；连续跑可授权，但不自动入台账
2. 脑洞本地；细纲/正文/去AI味/质检才走模型
3. 多模型按积分；作者确认模型后再写
4. 绑定辅助文档重读，不靠模型记设定
