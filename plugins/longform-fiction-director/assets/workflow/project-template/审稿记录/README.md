# 审稿记录

每次需要时再建记录。可检查：

- 与已确认正文、人物、设定、时间线和伏笔是否冲突。
- 称谓、地点、物件、数量、事件先后是否一致。
- 是否有重复解释、模板句、空泛判断、机械转场或人物说话失真。
- 改稿是否把原本的事件、人物选择和因果改掉。

审稿结论只给问题、依据和候选修改，不替作者自动定稿。

`模型写作记录.md` 由插件自动维护，索引每次外部模型生成的候选 txt。继续写作时可先看索引，再读取相关 `.body.txt`；它是过程记录，不是第二套故事事实台账。
